module.exports = {
    // Liste des chemins à ignorer pour la génération du coverage
    coveragePathIgnorePatterns: [
      './wake-up-api', // Chemin absolu vers le dossier à ignorer
    ],
  };
  
